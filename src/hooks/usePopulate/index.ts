/**
 * usePopulate Hook
 * Used to populate database. [Is it needed?]
 * @param a
 * @param b
 */
const usePopulate = (a: any, b: any) => {
    console.log('usePopulate: a: ', a, ' - b: ', b)
}

export default usePopulate